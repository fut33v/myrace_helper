x-service-base: &service-base
  build:
    context: .
  env_file:
    - .env
  volumes:
    - ./cookies:/app/cookies
    - ./data:/app/data
  restart: unless-stopped

services:
  bot:
    <<: *service-base
    command: ["python3", "telegram_bot.py"]

  scheduler:
    <<: *service-base
    command: ["python3", "race_income_watcher.py"]
